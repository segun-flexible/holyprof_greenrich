let submit, cbText;

document.addEventListener("DOMContentLoaded", () => {

    //SUBMIT FORM
    document.querySelector("form#edit-wallet").addEventListener("submit", e => {
        e.preventDefault();
        
    })
})